---
// src/pages/index.astro — a quiet newspaper-style personal record, pure black on pure white, full lowercase, serif.
import '../styles/global.css';

const today = new Date();
const dateStr = today.toLocaleDateString('en-GB', { day: 'numeric', month: 'long', year: 'numeric' });
---

<!doctype html>
<html lang="en" data-theme="light">
<head>
	<meta charset="utf-8" />
	<meta name="viewport" content="width=device-width, initial-scale=1" />
	<meta name="description" content="mehmet utku öztürk — software developer focused on artificial consciousness and thinking systems. projects, research, and contact." />
	<title>mehmet utku öztürk</title>
	<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
	<link rel="icon" type="image/x-icon" href="/favicon.ico" />
	<meta property="og:type" content="website" />
	<meta property="og:title" content="mehmet utku öztürk" />
	<meta property="og:description" content="software developer focused on artificial consciousness and thinking systems. projects, research, and contact." />
	<meta name="twitter:card" content="summary" />
	<meta name="twitter:title" content="mehmet utku öztürk" />
	<meta name="twitter:description" content="software developer focused on artificial consciousness and thinking systems." />
	<link rel="preconnect" href="https://fonts.googleapis.com" />
	<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
	<link href="https://fonts.googleapis.com/css2?family=Newsreader:ital,opsz,wght@0,6..72,400..600;1,6..72,400..500&family=Playfair+Display:ital,wght@0,500..800;1,500..600&display=swap" rel="stylesheet" />
</head>
<body data-theme="light">

<!-- reading-progress hairline -->
<div id="scrollProgress" aria-hidden="true"></div>

<!-- ═══ NAVBAR ════════════════════════════════════════════════════════════ -->
<nav id="navbar">
	<a href="#top" class="nav-brand">utku&rsquo;s&nbsp;space</a>
	<div class="nav-links">
		<a href="#about" class="nav-link">about</a>
		<a href="#projects" class="nav-link">projects</a>
		<a href="#research" class="nav-link">research</a>
		<a href="#contact" class="nav-link">contact</a>
	</div>
	<button class="mode-toggle" id="modeToggle" aria-label="toggle dark mode" title="day / night (d)">
		<svg class="icon-sun" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
			<circle cx="12" cy="12" r="4.5"></circle>
			<path d="M12 1.5v2.5M12 20v2.5M4.2 4.2l1.8 1.8M18 18l1.8 1.8M1.5 12h2.5M20 12h2.5M4.2 19.8l1.8-1.8M18 6l1.8-1.8"></path>
		</svg>
		<svg class="icon-moon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
			<path d="M21 12.8A9 9 0 1 1 11.2 3 7 7 0 0 0 21 12.8z"></path>
		</svg>
	</button>
</nav>

<!-- ═══ PAGE ══════════════════════════════════════════════════════════════ -->
<main class="paper" id="top">

	<!-- ── MASTHEAD ───────────────────────────────────────────────────── -->
	<header class="masthead reveal">
		<div class="dateline">
			<span>ankara &middot; türkiye</span>
			<span id="datelineDate">{dateStr}</span>
		</div>
		<h1 class="nameplate">mehmet utku öztürk</h1>
	</header>

	<!-- ── LEAD (bio + interests + portrait) ──────────────────────────── -->
	<section class="lead reveal" aria-label="introduction">
		<div class="lead-body">
			<p class="standfirst">started programming when i was 8 with small apps and scripts. got familiar with c++ and c# by i was 11. started working on machine learning at 13, extremely unsuccessfully finetuned my first llm at 14. since then, i&rsquo;m working on software with a primary focus on artificial consciousness and &ldquo;thinking&rdquo; systems.</p>
			<p class="interests">other than software, i am interested in powerlifting, strongman, martial arts, cognitive science, kinesiology, chess, sudoku, and good coffee.</p>
		</div>

		<aside class="lead-aside">
			<figure class="fig">
				<div class="fig-slot">
					<img src="/portrait.png" alt="mehmet utku öztürk — black-and-white engraved portrait" />
				</div>
				<figcaption>mehmet utku öztürk. <span class="cap-soft">ankara, türkiye.</span></figcaption>
			</figure>
		</aside>
	</section>

	<!-- ── ABOUT ──────────────────────────────────────────────────────── -->
	<section id="about" class="section reveal">
		<div class="rule-head"><h2 class="rule-head-label">about</h2></div>
		<div class="cols cols-2">
			<div class="col">
				<article class="item">
					<button class="item-head" aria-expanded="false" onclick="toggleCard(this)">
						<span class="item-title">lifelong software developer</span>
						<span class="item-byline">work</span>
						<span class="item-plus" aria-hidden="true"></span>
					</button>
					<div class="item-body"><div class="item-body-inner"><p>writing code since age 8 — small games, tools, experiments. picked up c++ at 12 and fell into machine learning at 14. haven&rsquo;t stopped since.</p></div></div>
				</article>
				<article class="item">
					<button class="item-head" aria-expanded="false" onclick="toggleCard(this)">
						<span class="item-title">machine learning specialist</span>
						<span class="item-byline">field</span>
						<span class="item-plus" aria-hidden="true"></span>
					</button>
					<div class="item-body"><div class="item-body-inner"><p>deep work in nlp, agentic systems, and production ml. shipped systems across several startups. currently researching neuro-symbolic ai and artificial cognitive intelligence.</p></div></div>
				</article>
				<article class="item">
					<button class="item-head" aria-expanded="false" onclick="toggleCard(this)">
						<span class="item-title">class ii powerlifter</span>
						<span class="item-byline">physical</span>
						<span class="item-plus" aria-hidden="true"></span>
					</button>
					<div class="item-body"><div class="item-body-inner"><p>competitive class ii powerlifter — 1000+ lb total. training hybrid: powerlifting, strongman, bodybuilding, calisthenics, and kickboxing.</p></div></div>
				</article>
			</div>
			<div class="col">
				<article class="item">
					<button class="item-head" aria-expanded="false" onclick="toggleCard(this)">
						<span class="item-title">founder @ 23rd the company</span>
						<span class="item-byline">social</span>
						<span class="item-plus" aria-hidden="true"></span>
					</button>
					<div class="item-body"><div class="item-body-inner"><p>founder of 23rd — a decentralized open-innovation collective on ethereum, conducting and openly publishing meaningful work, mostly in machine learning.</p></div></div>
				</article>
				<article class="item">
					<button class="item-head" aria-expanded="false" onclick="toggleCard(this)">
						<span class="item-title">member @ mensa international</span>
						<span class="item-byline">community</span>
						<span class="item-plus" aria-hidden="true"></span>
					</button>
					<div class="item-body"><div class="item-body-inner"><p>member of the mensa high iq society. a fun credential — and a good place to meet sharp, curious people.</p></div></div>
				</article>
				<article class="item">
					<button class="item-head" aria-expanded="false" onclick="toggleCard(this)">
						<span class="item-title">mathematics, psychoanalysis, kinesiology, neuroscience</span>
						<span class="item-byline">interests</span>
						<span class="item-plus" aria-hidden="true"></span>
					</button>
					<div class="item-body"><div class="item-body-inner"><p>topics i keep reading about — curious, not expert.</p></div></div>
				</article>
			</div>
		</div>
	</section>

	<!-- ── PROJECTS ───────────────────────────────────────────────────── -->
	<section id="projects" class="section reveal">
		<div class="rule-head"><h2 class="rule-head-label">projects</h2></div>
		<div class="cols cols-2">
			<div class="col">
				<article class="item item-proj">
					<button class="item-head" aria-expanded="false" onclick="toggleCard(this)">
						<span class="item-title"><span class="status status-live" aria-label="active"></span>turkhukuk.ai</span>
						<span class="item-byline">technical co-founder &middot; 2026 —</span>
						<span class="item-plus" aria-hidden="true"></span>
					</button>
					<div class="item-body"><div class="item-body-inner"><p>a legaltech platform for turkish lawyers — case research and litigation strategy. trained and fine-tuned domain-specific language models on turkish legal corpora, and experimented with neuro-symbolic representations to bridge statistical learning with structured legal reasoning. <a href="https://www.turkhukuk.ai" target="_blank" rel="noopener" class="chip">turkhukuk.ai ↗</a></p></div></div>
				</article>
				<article class="item item-proj">
					<button class="item-head" aria-expanded="false" onclick="toggleCard(this)">
						<span class="item-title"><span class="status status-live" aria-label="active"></span>007captcha</span>
						<span class="item-byline">creator &middot; 2026 — &middot; open source</span>
						<span class="item-plus" aria-hidden="true"></span>
					</button>
					<div class="item-body"><div class="item-body-inner"><p>a robophobic verification framework that defends websites against ai agents. an open-source take on captcha built for the post-llm web, where classical challenges no longer hold. typescript. mit licensed. <a href="https://github.com/mutkuoz/007captcha" target="_blank" rel="noopener" class="chip">github ↗</a></p></div></div>
				</article>
				<article class="item item-proj">
					<button class="item-head" aria-expanded="false" onclick="toggleCard(this)">
						<span class="item-title"><span class="status status-paused" aria-label="paused"></span>octapar</span>
						<span class="item-byline">founder &amp; core dev &middot; 2025</span>
						<span class="item-plus" aria-hidden="true"></span>
					</button>
					<div class="item-body"><div class="item-body-inner"><p>an ai platform that clusters people into 8-person &ldquo;tribes&rdquo; — balanced, semantically coherent groups for authentic connection and mutual growth. designed a multi-stage clustering algorithm around weighted attentional embeddings and the hungarian method. paper published via 23rd.</p></div></div>
				</article>
			</div>
			<div class="col">
				<article class="item item-proj">
					<button class="item-head" aria-expanded="false" onclick="toggleCard(this)">
						<span class="item-title"><span class="status status-shelved" aria-label="shelved"></span>clade ai</span>
						<span class="item-byline">co-founder, ai dev &middot; 2023–24</span>
						<span class="item-plus" aria-hidden="true"></span>
					</button>
					<div class="item-body"><div class="item-body-inner"><p>a personal ai assistant with task execution, extensive integrations, and personalized interaction. raised a $21m round. built a self-deciding multi-agent framework in the early days of ai agents, fine-tuned an llm for natural interaction, and shipped custom systems for smart memory, decision-making, and seamless integrations. left in july 2024; shelved shortly after.</p></div></div>
				</article>
				<article class="item item-proj">
					<button class="item-head" aria-expanded="false" onclick="toggleCard(this)">
						<span class="item-title"><span class="status status-shelved" aria-label="shelved"></span>sorubot</span>
						<span class="item-byline">developer &middot; 2022–23</span>
						<span class="item-plus" aria-hidden="true"></span>
					</button>
					<div class="item-body"><div class="item-body-inner"><p>an ai platform that generates exam questions and manages students&rsquo; study sessions. very unsuccessfully fine-tuned an llm. amateur work — but not bad for a 14-year-old.</p></div></div>
				</article>
				<article class="item item-proj">
					<button class="item-head" aria-expanded="false" onclick="toggleCard(this)">
						<span class="item-title"><span class="status status-shelved" aria-label="shelved"></span>styrk</span>
						<span class="item-byline">co-founder, backend dev &middot; 2025</span>
						<span class="item-plus" aria-hidden="true"></span>
					</button>
					<div class="item-body"><div class="item-body-inner"><p>sports supplements with ai-driven personalization and ai-generated packaging. a fun weekend project. shelved.</p></div></div>
				</article>
			</div>
		</div>
		<p class="legend"><span class="status status-live"></span> active &nbsp;·&nbsp; <span class="status status-paused"></span> paused &nbsp;·&nbsp; <span class="status status-shelved"></span> shelved</p>
	</section>

	<!-- ── RESEARCH ───────────────────────────────────────────────────── -->
	<section id="research" class="section reveal">
		<div class="rule-head"><h2 class="rule-head-label">research</h2></div>
		<div class="papers">
			<article class="item item-paper">
				<button class="item-head" aria-expanded="false" onclick="toggleCard(this)">
					<span class="item-title">HukukBERT Domain-Specific Language Model for Turkish Law</span>
					<span class="item-byline">turkhukuk.ai &middot; 2026</span>
					<span class="item-plus" aria-hidden="true"></span>
				</button>
				<div class="item-body"><div class="item-body-inner"><p class="abstract">Recent advances in natural language processing (NLP) have opened significant doors for LegalTech applications; however, existing studies specific to Turkish law have been severely limited by data constraints. While extensive models like LEGAL-BERT have been developed for English legal texts, the Turkish legal domain lacks a high-volume, domain-specific counterpart. In this paper, we introduce HukukBERT, the most comprehensive legal language model for Turkish, trained on an 18 GB cleaned legal corpus using advanced masking strategies. We systematically compare our 48K WordPiece tokenizer and Domain-Adaptive Pre-Training (DAPT) approach against general-purpose and existing domain-specific Turkish models. Evaluated on a novel Legal Cloze Test benchmark, HukukBERT achieves state-of-the-art performance with 84.40% Top-1 accuracy, substantially outperforming existing models such as BERTurk-Legal. We release HukukBERT to serve as a foundational model for downstream Turkish legal tasks.</p><a href="https://arxiv.org/abs/2604.04790" target="_blank" rel="noopener" class="chip">arxiv ↗</a></div></div>
			</article>
			<article class="item item-paper">
				<button class="item-head" aria-expanded="false" onclick="toggleCard(this)">
					<span class="item-title">Octapar: A Framework for Multi-Modal, Balanced User Grouping using Weighted Attentional Embeddings and Assignment-Based Clustering</span>
					<span class="item-byline">23rd the company &middot; 2025</span>
					<span class="item-plus" aria-hidden="true"></span>
				</button>
				<div class="item-body"><div class="item-body-inner"><p class="abstract">The formation of coherent, effective, and equitably sized user groups is a critical and persistent challenge in the design of online social platforms, collaborative learning environments, and enterprise-level teamwork tools. Existing automated methods, while scalable, often fail to produce groups of a consistent, predetermined size, leading to significant logistical issues and imbalanced user experiences. Furthermore, they often rely on shallow or easily manipulated user data. This paper introduces Octapar, a novel, multi-stage algorithmic framework designed to generate semantically coherent, perfectly balanced user clusters from deep, multi-modal user profiles. The framework pioneers a conversational onboarding process, using specialized chatbots and Large Language Models (LLMs) to extract nuanced user characteristics across both skill and philosophical domains. These characteristics are formalized as a rich set of semantic tags. The core technical innovation is a custom weighted attention mechanism that generates a sophisticated user vector by incorporating not only global tag rarity (IDF) but also a novel metric of local, intra-user tag uniqueness. This allows the model to identify and amplify a user's most distinctive traits. Final group formation is achieved via an iterative, assignment-based clustering algorithm that utilizes the Hungarian method to guarantee perfectly balanced clusters. This approach ensures structural consistency and fairness in group formation, a critical and often unmet requirement for many practical applications. We provide a detailed walkthrough of the architecture, the mathematical foundations of its components, and a discussion of its implications for the future of computational group formation.</p><a href="https://zenodo.org/records/17360220" target="_blank" rel="noopener" class="chip">zenodo ↗</a></div></div>
			</article>
			<article class="item item-paper">
				<button class="item-head" aria-expanded="false" onclick="toggleCard(this)">
					<span class="item-title">Streaming Intelligence: Harnessing AI and Big Data in the Competitive Landscape of Digital Content Platforms</span>
					<span class="item-byline">Plawlabs &middot; 2024</span>
					<span class="item-plus" aria-hidden="true"></span>
				</button>
				<div class="item-body"><div class="item-body-inner"><p class="abstract">In the rapidly evolving realm of digital entertainment, Artificial Intelligence (AI) and Big Data stand at the forefront, reshaping the future of content consumption and curation. Esteemed global streaming giants such as Netflix, YouTube, Hulu, Amazon Prime Video, HBO Max, Disney+, and even social platforms like Instagram and TikTok, are skillfully leveraging these technologies to transform content recommendation, presentation, and personalization processes. This not only elevates the user experience but also sets unprecedented standards for operational efficiency across the industry. Our article delves into the intricate use of AI and Big Data by these leading platforms, highlighting their critical role in captivating audiences and streamlining content strategies. Furthermore, our research offers concrete insights and forward-looking strategies for one of the Turkey's emerging digital platform, in collaboration with PlawLabs, on surpassing its global competitors with the development of advanced AI technology. This initiative is aimed at overcoming unique challenges brought forth by these technological advancements and seizing expansive opportunities. By capitalizing on the technological expertise and strategic foresights of current industry leaders, this platform is poised to gain a significant competitive edge in the rapidly growing market of digital streaming services, both domestically and internationally. Adopting our technology foresights will not only enhance the quality of content and increase user engagement but will also seamlessly align with the changing preferences and demands of today's diverse digital audience, supporting the platform's financial outcomes. Through this comprehensive analysis, we aim to illuminate a path for our national platform to leverage AI and Big Data, catalyze its growth, and redefine content maturity in the digital space.</p></div></div>
			</article>
		</div>
		<p class="more-note">more coming.</p>
	</section>

	<!-- ── CONTACT ────────────────────────────────────────────────────── -->
	<section id="contact" class="section reveal">
		<div class="rule-head"><h2 class="rule-head-label">contact</h2></div>
		<p class="contact-intro">open to collaboration, research, and good conversation.</p>
		<p class="contact-loc">ankara, türkiye &middot; gmt+3</p>
		<dl class="contact-list">
			<a href="mailto:mutkuoz@proton.me" class="c-link"><dt>email</dt><dd>mutkuoz@proton.me</dd></a>
			<a href="mailto:utku.ozturk@member.mensa.org" class="c-link"><dt>mensa</dt><dd>utku.ozturk@member.mensa.org</dd></a>
			<a href="https://github.com/mutkuoz" target="_blank" rel="noopener" class="c-link"><dt>github</dt><dd>@mutkuoz</dd></a>
			<a href="https://linkedin.com/in/mutkuoz" target="_blank" rel="noopener" class="c-link"><dt>linkedin</dt><dd>@mutkuoz</dd></a>
		</dl>
	</section>

	<!-- ── COLOPHON ───────────────────────────────────────────────────── -->
	<footer class="colophon reveal">
		<p class="colophon-line">the personal record of mehmet utku öztürk. set in playfair display &amp; newsreader. last revised june 2026.</p>
		<p class="colophon-line muted">diversions — <button class="ghost-link" id="playLink">snake</button></p>
	</footer>

</main>

<!-- small toast for game feedback -->
<div class="toast" id="toast" aria-live="polite"></div>

<!-- ═══ SNAKE MINIGAME ════════════════════════════════════════════════════ -->
<div class="snake" id="snake" aria-hidden="true">
	<canvas id="snakeCanvas"></canvas>
	<div class="snake-bar">
		<span class="snake-title">snake</span>
		<span class="snake-score">score <b id="snakeScore">0</b></span>
		<span class="snake-keys">swipe / arrows to steer</span>
		<button class="snake-close" id="snakeClose" aria-label="quit snake">✕</button>
	</div>
	<div class="snake-over" id="snakeOver">
		<p class="snake-over-t">game over</p>
		<p class="snake-over-s">score <b id="snakeFinal">0</b></p>
		<button class="snake-btn" id="snakeRetry">play again</button>
	</div>
</div>

<!-- ═══ STYLES ════════════════════════════════════════════════════════════ -->
<style>
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

:root {
	--serif:   'Newsreader', Georgia, 'Times New Roman', serif;
	--display: 'Playfair Display', Georgia, 'Times New Roman', serif;
	/* alias consumed by snake canvas */
	--ink: var(--fg);
}

/* ── pure black / white theming ────────────────────────────────────── */
[data-theme="light"] {
	--bg:#ffffff; --fg:#000000;
	--fg2:rgba(0,0,0,0.74); --muted:rgba(0,0,0,0.52); --muted2:rgba(0,0,0,0.36);
	--hair:rgba(0,0,0,0.16);
}
[data-theme="dark"] {
	--bg:#000000; --fg:#ffffff;
	--fg2:rgba(255,255,255,0.78); --muted:rgba(255,255,255,0.54); --muted2:rgba(255,255,255,0.38);
	--hair:rgba(255,255,255,0.20);
}

html { scroll-behavior: smooth; }

body {
	font-family: var(--serif);
	font-optical-sizing: auto;
	text-transform: lowercase;
	color: var(--fg);
	background: var(--bg);
	-webkit-font-smoothing: antialiased;
	-moz-osx-font-smoothing: grayscale;
	transition: background-color 0.35s ease, color 0.35s ease;
	min-height: 100vh;
	line-height: 1.5;
}

::selection { background: var(--fg); color: var(--bg); }

/* ── reading progress ──────────────────────────────────────────────── */
#scrollProgress {
	position: fixed; top: 0; left: 0; height: 2px; width: 0;
	background: var(--fg); z-index: 200; opacity: 0.6;
}

/* ── navbar ────────────────────────────────────────────────────────── */
#navbar {
	position: fixed; top: 0; left: 0; right: 0; z-index: 100;
	display: flex; align-items: center; justify-content: space-between;
	gap: 1rem; height: 3.4rem; padding: 0 1.3rem;
	background: color-mix(in srgb, var(--bg) 86%, transparent);
	backdrop-filter: blur(7px); -webkit-backdrop-filter: blur(7px);
	border-bottom: 1px solid var(--hair);
	transition: background 0.35s ease, border-color 0.35s ease;
}
@media (min-width: 720px) { #navbar { padding: 0 2rem; } }
.nav-brand {
	font-family: var(--display); font-style: italic; font-weight: 600;
	font-size: 1.02rem; color: var(--fg); text-decoration: none; white-space: nowrap;
}
.nav-links { display: none; gap: 1.6rem; margin-left: auto; margin-right: 1.3rem; }
@media (min-width: 700px) { .nav-links { display: flex; } }
.nav-link {
	font-family: var(--serif); font-size: 0.86rem; letter-spacing: 0.02em;
	color: var(--muted); text-decoration: none;
	position: relative; padding-bottom: 2px; transition: color 0.18s ease;
}
.nav-link::after {
	content: ''; position: absolute; left: 0; right: 100%; bottom: 0; height: 1px;
	background: var(--fg); transition: right 0.25s cubic-bezier(0.4,0,0.2,1);
}
.nav-link:hover { color: var(--fg); }
.nav-link:hover::after { right: 0; }

.mode-toggle {
	display: flex; align-items: center; justify-content: center;
	width: 2rem; height: 2rem; border-radius: 50%;
	border: 1px solid var(--hair); background: transparent; cursor: pointer; color: var(--fg);
	transition: border-color 0.2s ease, transform 0.4s cubic-bezier(0.34,1.4,0.64,1);
}
.mode-toggle:hover { border-color: var(--fg); transform: rotate(-22deg); }
.mode-toggle:focus-visible { outline: 2px solid var(--fg); outline-offset: 2px; }
.mode-toggle svg { width: 0.95rem; height: 0.95rem; }
[data-theme="light"] .icon-moon { display: none; }
[data-theme="dark"]  .icon-sun  { display: none; }

/* ── page column (wider) ───────────────────────────────────────────── */
.paper {
	max-width: 80rem; margin: 0 auto;
	padding: 6rem 1.3rem 4rem;
}
@media (min-width: 720px) { .paper { padding: 7rem 2.4rem 5rem; } }
.section { scroll-margin-top: 4.5rem; margin-top: 3.4rem; }

/* ── masthead ──────────────────────────────────────────────────────── */
.masthead { border-bottom: 1px solid var(--hair); padding-bottom: 1.2rem; }
.dateline {
	display: flex; align-items: center; justify-content: space-between; gap: 0.6rem;
	font-size: 0.72rem; letter-spacing: 0.05em; color: var(--muted); margin-bottom: 0.9rem;
}
.nameplate {
	font-family: var(--display); font-weight: 700;
	font-size: clamp(2.4rem, 8vw, 5rem); line-height: 1; text-align: center;
	letter-spacing: -0.02em;
}

/* ── lead block ────────────────────────────────────────────────────── */
.lead { display: grid; grid-template-columns: 1fr; gap: 2rem; margin-top: 2.2rem; }
@media (min-width: 840px) {
	.lead { grid-template-columns: 1.5fr 1fr; gap: 3rem; align-items: start; }
}
.standfirst {
	font-size: clamp(1.15rem, 1.7vw, 1.5rem); line-height: 1.8; color: var(--fg);
	text-align: justify; hyphens: auto;
}
.interests {
	margin-top: 1.1rem; font-style: italic; font-size: 1.08rem; line-height: 1.72; color: var(--fg2);
}

/* portrait — transparent line-art cutout, floats on the page */
.fig { margin: 0; }
.fig-slot { position: relative; }
.fig-slot img { display: block; width: 100%; height: auto; }
/* black engraving would vanish on a black page — invert it in dark mode */
[data-theme="dark"] .fig-slot img { filter: invert(1); }
figcaption {
	font-style: italic; font-size: 0.82rem; color: var(--muted);
	padding-top: 0.5rem; margin-top: 0.5rem; border-top: 1px solid var(--hair); line-height: 1.5;
}
.cap-soft { color: var(--muted2); }

/* ── section rule-head (quiet) ─────────────────────────────────────── */
.rule-head { margin-bottom: 1.4rem; padding-bottom: 0.45rem; border-bottom: 1px solid var(--hair); }
.rule-head-label { font-family: var(--serif); font-size: 1rem; font-weight: 600; letter-spacing: 0.01em; color: var(--fg); }

/* ── columns ───────────────────────────────────────────────────────── */
.cols-2 { display: grid; grid-template-columns: 1fr; }
@media (min-width: 760px) { .cols-2 { grid-template-columns: 1fr 1fr; column-gap: 3.2rem; } }

/* ── expandable item ───────────────────────────────────────────────── */
.item { border-top: 1px solid var(--hair); }
.col > .item:first-child, .papers > .item:first-child { border-top: none; }
@media (max-width: 759px) { .col + .col > .item:first-child { border-top: 1px solid var(--hair); } }

.item-head {
	display: grid; grid-template-columns: 1fr auto; grid-template-areas: "title plus" "byline plus";
	gap: 0 0.8rem; align-items: start; width: 100%; text-align: left;
	background: none; border: none; cursor: pointer; color: inherit; font-family: inherit;
	padding: 0.95rem 0; transition: padding-left 0.25s cubic-bezier(0.4,0,0.2,1);
}
.item-head:hover { padding-left: 0.4rem; }
.item-head:focus-visible { outline: 2px solid var(--fg); outline-offset: 2px; }
.item-title {
	grid-area: title; font-size: 1.04rem; font-weight: 600; line-height: 1.34; color: var(--fg);
	display: inline-flex; align-items: baseline; gap: 0.5rem;
}
.item-byline {
	grid-area: byline; margin-top: 0.25rem;
	font-size: 0.74rem; letter-spacing: 0.02em; color: var(--muted);
}
.item-plus { grid-area: plus; align-self: center; width: 1rem; text-align: center; }
.item-plus::after {
	content: '+'; font-family: var(--display); font-size: 1.3rem; line-height: 1; color: var(--muted);
	transition: color 0.18s ease; display: inline-block;
}
.item-head:hover .item-plus::after { color: var(--fg); }
.item-head[aria-expanded="true"] .item-plus::after { content: '\2212'; color: var(--fg); }

.item-proj .item-title { font-family: var(--display); font-weight: 700; font-size: 1.2rem; letter-spacing: -0.01em; }
.item-paper .item-title { font-style: italic; font-weight: 500; font-size: 1.08rem; }

/* status markers */
.status { display: inline-block; width: 0.6rem; height: 0.6rem; flex-shrink: 0; transform: translateY(-0.04em); }
.status-live { background: var(--fg); }
.status-paused { background: transparent; box-shadow: inset 0 0 0 1.5px var(--fg); }
.status-shelved { background: var(--muted2); }

.item-body { display: grid; grid-template-rows: 0fr; transition: grid-template-rows 0.32s cubic-bezier(0.4,0,0.2,1); }
.item-head[aria-expanded="true"] + .item-body { grid-template-rows: 1fr; }
.item-body-inner { overflow: hidden; min-height: 0; }
.item-body p {
	font-size: 0.95rem; line-height: 1.8; color: var(--fg2); max-width: 48rem;
	padding: 0.1rem 0 1rem;
}
.item-body p.abstract { text-align: justify; hyphens: auto; max-width: 64rem; }

/* link chip */
.chip {
	display: inline-block; margin-bottom: 1rem;
	font-size: 0.78rem; letter-spacing: 0.02em; color: var(--fg);
	text-decoration: none; border-bottom: 1px solid var(--fg); padding-bottom: 1px;
	transition: opacity 0.18s ease;
}
.item-body p + .chip { margin-top: -0.4rem; }
.chip:hover { opacity: 0.55; }

.legend { margin-top: 1.4rem; font-size: 0.8rem; color: var(--muted); display: flex; align-items: center; gap: 0.3rem; flex-wrap: wrap; }
.legend .status { margin-right: 0.1rem; }
.more-note { margin-top: 1.4rem; font-style: italic; font-size: 0.9rem; color: var(--muted2); }
.papers .item-body p.abstract { padding-bottom: 0.7rem; }

/* ── contact ───────────────────────────────────────────────────────── */
.contact-intro { font-size: 1.15rem; line-height: 1.6; color: var(--fg); max-width: 36rem; }
.contact-loc { margin-top: 0.5rem; margin-bottom: 1.6rem; font-size: 0.74rem; letter-spacing: 0.04em; color: var(--muted); }
.contact-list { display: grid; }
.c-link {
	display: grid; grid-template-columns: 6rem 1fr; align-items: baseline; gap: 1rem;
	padding: 0.85rem 0; border-top: 1px solid var(--hair); text-decoration: none; color: var(--fg);
	transition: padding-left 0.25s cubic-bezier(0.4,0,0.2,1);
}
.c-link:last-child { border-bottom: 1px solid var(--hair); }
.c-link:hover { padding-left: 0.5rem; }
.c-link dt { font-size: 0.74rem; letter-spacing: 0.03em; color: var(--muted); }
.c-link:hover dt { color: var(--fg); }
.c-link dd { font-size: 1rem; font-weight: 500; }
.c-link:hover dd { text-decoration: underline; text-underline-offset: 3px; }

/* ── colophon ──────────────────────────────────────────────────────── */
.colophon { margin-top: 4rem; padding-top: 1.1rem; border-top: 1px solid var(--hair); }
.colophon-line { font-style: italic; font-size: 0.82rem; color: var(--muted); line-height: 1.9; }
.colophon-line.muted { font-style: normal; letter-spacing: 0.02em; }
.ghost-link {
	font-family: var(--serif); font-size: 0.82rem; color: var(--fg);
	background: none; border: none; border-bottom: 1px solid var(--hair); cursor: pointer;
	padding: 0 0 1px; transition: border-color 0.18s ease;
}
.ghost-link:hover { border-color: var(--fg); }

/* ── toast ─────────────────────────────────────────────────────────── */
.toast {
	position: fixed; left: 50%; bottom: 2rem; transform: translateX(-50%) translateY(1.5rem);
	z-index: 450; padding: 0.55rem 1.1rem;
	background: var(--fg); color: var(--bg); font-size: 0.8rem; font-style: italic;
	opacity: 0; pointer-events: none; white-space: nowrap;
	transition: opacity 0.25s ease, transform 0.3s cubic-bezier(0.34,1.4,0.64,1);
}
.toast.show { opacity: 1; transform: translateX(-50%) translateY(0); }

/* ── snake minigame ────────────────────────────────────────────────── */
.snake {
	position: fixed; inset: 0; z-index: 400; display: none;
	background: color-mix(in srgb, var(--bg) 93%, transparent);
	backdrop-filter: blur(2px); -webkit-backdrop-filter: blur(2px);
}
.snake.show { display: block; }
#snakeCanvas { position: absolute; inset: 0; width: 100%; height: 100%; display: block; touch-action: none; }
.snake-bar {
	position: absolute; top: 1.2rem; left: 50%; transform: translateX(-50%);
	display: flex; align-items: center; gap: 1.3rem; font-size: 0.82rem; color: var(--muted);
	padding: 0.5rem 1.1rem; background: color-mix(in srgb, var(--bg) 75%, transparent);
	border: 1px solid var(--hair);
}
.snake-title { font-weight: 600; color: var(--fg); letter-spacing: 0.06em; }
.snake-score b { color: var(--fg); }
.snake-keys { color: var(--muted2); }
.snake-close {
	display: flex; align-items: center; justify-content: center; width: 1.5rem; height: 1.5rem;
	margin-left: 0.2rem; padding: 0; border: 1px solid var(--hair); border-radius: 50%;
	background: transparent; color: var(--muted); cursor: pointer; font-size: 0.8rem; line-height: 1;
	transition: color 0.18s ease, border-color 0.18s ease;
}
.snake-close:hover { color: var(--fg); border-color: var(--fg); }
@media (max-width: 480px) { .snake-keys { display: none; } }
.snake-over {
	position: absolute; top: 50%; left: 50%; transform: translate(-50%,-50%) scale(0.9);
	text-align: center; display: none; opacity: 0;
	transition: opacity 0.25s ease, transform 0.3s cubic-bezier(0.34,1.4,0.64,1);
}
.snake-over.show { display: block; opacity: 1; transform: translate(-50%,-50%) scale(1); }
.snake-over-t { font-family: var(--display); font-size: 2.2rem; font-weight: 700; color: var(--fg); margin-bottom: 0.4rem; }
.snake-over-s { font-size: 0.9rem; color: var(--muted); margin-bottom: 1.2rem; }
.snake-over-s b { color: var(--fg); }
.snake-btn {
	font-size: 0.82rem; letter-spacing: 0.04em;
	padding: 0.55rem 1.3rem; border: 1.5px solid var(--fg); background: transparent; color: var(--fg);
	cursor: pointer; transition: background 0.18s ease, color 0.18s ease;
}
.snake-btn:hover { background: var(--fg); color: var(--bg); }

/* ── reveal ────────────────────────────────────────────────────────── */
.reveal { opacity: 0; transform: translateY(14px); transition: opacity 0.6s cubic-bezier(0.4,0,0.2,1), transform 0.6s cubic-bezier(0.4,0,0.2,1); }
.reveal.visible { opacity: 1; transform: translateY(0); }

@media (prefers-reduced-motion: reduce) {
	html { scroll-behavior: auto; }
	.reveal { opacity: 1; transform: none; transition: none; }
}
</style>

<!-- ═══ SCRIPT ════════════════════════════════════════════════════════════ -->
<script is:inline>
/* ── accordion ─────────────────────────────────────────────────────── */
function toggleCard(btn) {
	btn.setAttribute('aria-expanded', String(btn.getAttribute('aria-expanded') !== 'true'));
}

/* ── small toast ───────────────────────────────────────────────────── */
const toast = (function () {
	const el = document.getElementById('toast');
	let t = 0;
	return function (msg, ms) {
		el.textContent = msg; el.classList.add('show');
		clearTimeout(t); t = setTimeout(() => el.classList.remove('show'), ms || 2200);
	};
})();

/* ── current-date dateline ─────────────────────────────────────────── */
(function () {
	const el = document.getElementById('datelineDate');
	if (el) el.textContent = new Date().toLocaleDateString('en-GB', { day: 'numeric', month: 'long', year: 'numeric' });
})();

/* ── day / night (light is default) ────────────────────────────────── */
(function () {
	const html = document.documentElement;
	const saved = localStorage.getItem('theme') || 'light';
	html.setAttribute('data-theme', saved); document.body.setAttribute('data-theme', saved);
	document.getElementById('modeToggle').addEventListener('click', () => {
		const next = html.getAttribute('data-theme') === 'dark' ? 'light' : 'dark';
		html.setAttribute('data-theme', next); document.body.setAttribute('data-theme', next);
		localStorage.setItem('theme', next);
	});
})();

/* ── ink helper (foreground colour) for snake canvas ───────────────── */
function inkColor() {
	return getComputedStyle(document.documentElement).getPropertyValue('--fg').trim()
		|| (document.documentElement.getAttribute('data-theme') === 'dark' ? '#ffffff' : '#000000');
}

/* ── reveal on scroll ──────────────────────────────────────────────── */
(function () {
	const io = new IntersectionObserver(es => es.forEach(e => { if (e.isIntersecting) { e.target.classList.add('visible'); io.unobserve(e.target); } }), { threshold: 0.05 });
	document.querySelectorAll('.reveal').forEach((el, i) => { el.style.transitionDelay = (i * 0.04) + 's'; io.observe(el); });
})();

/* ── scroll progress ───────────────────────────────────────────────── */
(function () {
	const bar = document.getElementById('scrollProgress'); let raf = 0;
	function up() { const h = document.documentElement, m = h.scrollHeight - h.clientHeight; bar.style.width = (m > 0 ? (h.scrollTop / m) * 100 : 0) + '%'; raf = 0; }
	addEventListener('scroll', () => { if (!raf) raf = requestAnimationFrame(up); }, { passive: true }); up();
})();

/* ── SNAKE minigame ────────────────────────────────────────────────── */
const Snake = (function () {
	const wrap = document.getElementById('snake');
	const canvas = document.getElementById('snakeCanvas');
	const ctx = canvas.getContext('2d');
	const scoreEl = document.getElementById('snakeScore');
	const overEl = document.getElementById('snakeOver');
	const finalEl = document.getElementById('snakeFinal');
	const CELL = 26;
	let cols, rows, snake, dir, nextDir, food, score, dead, acc, lastT, raf = 0, running = false, ox = 0, oy = 0;

	function fit() {
		const dpr = Math.min(window.devicePixelRatio || 1, 2);
		canvas.width = innerWidth * dpr; canvas.height = innerHeight * dpr;
		canvas.style.width = innerWidth + 'px'; canvas.style.height = innerHeight + 'px';
		ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
		cols = Math.floor(innerWidth / CELL); rows = Math.floor(innerHeight / CELL);
		ox = (innerWidth - cols * CELL) / 2; oy = (innerHeight - rows * CELL) / 2;
	}
	function placeFood() {
		do { food = { x: (Math.random() * cols) | 0, y: (Math.random() * rows) | 0 }; }
		while (snake.some(s => s.x === food.x && s.y === food.y));
	}
	function reset() {
		fit();
		const cx = cols >> 1, cy = rows >> 1;
		snake = [{ x: cx, y: cy }, { x: cx - 1, y: cy }, { x: cx - 2, y: cy }];
		dir = { x: 1, y: 0 }; nextDir = dir; score = 0; dead = false; acc = 0; lastT = 0;
		scoreEl.textContent = '0'; overEl.classList.remove('show'); placeFood();
	}
	function step() {
		dir = nextDir;
		const head = { x: snake[0].x + dir.x, y: snake[0].y + dir.y };
		if (head.x < 0 || head.y < 0 || head.x >= cols || head.y >= rows || snake.some(s => s.x === head.x && s.y === head.y)) {
			dead = true; finalEl.textContent = score; overEl.classList.add('show'); return;
		}
		snake.unshift(head);
		if (head.x === food.x && head.y === food.y) { score++; scoreEl.textContent = score; placeFood(); }
		else snake.pop();
	}
	function cell(x, y) { return [ox + x * CELL, oy + y * CELL]; }
	function render() {
		ctx.clearRect(0, 0, innerWidth, innerHeight);
		const ink = inkColor();
		const [fx, fy] = cell(food.x, food.y);
		ctx.fillStyle = ink; ctx.globalAlpha = 0.85;
		ctx.beginPath(); ctx.arc(fx + CELL / 2, fy + CELL / 2, CELL * 0.22, 0, 7); ctx.fill();
		ctx.globalAlpha = 1; ctx.strokeStyle = ink; ctx.lineWidth = CELL * 0.5; ctx.lineJoin = 'round'; ctx.lineCap = 'round';
		ctx.beginPath();
		snake.forEach((s, i) => { const [x, y] = cell(s.x, s.y); const px = x + CELL / 2, py = y + CELL / 2; i ? ctx.lineTo(px, py) : ctx.moveTo(px, py); });
		ctx.stroke();
		const [hx, hy] = cell(snake[0].x, snake[0].y);
		ctx.fillStyle = ink; ctx.beginPath(); ctx.arc(hx + CELL / 2, hy + CELL / 2, CELL * 0.3, 0, 7); ctx.fill();
	}
	function loop(t) {
		if (!running) return;
		if (!lastT) lastT = t;
		const dt = t - lastT; lastT = t;
		if (!dead) {
			acc += dt; const speed = Math.max(70, 130 - score * 3);
			while (acc >= speed) { acc -= speed; step(); if (dead) break; }
			render();
		}
		raf = requestAnimationFrame(loop);
	}
	function onKey(e) {
		const k = e.key;
		if (k === 'Escape' || k === 'q') { stop(); return; }
		if (dead && (k === ' ' || k === 'Enter')) { e.preventDefault(); reset(); return; }
		let nd = null;
		if (k === 'ArrowUp' || k === 'w') nd = { x: 0, y: -1 };
		else if (k === 'ArrowDown' || k === 's') nd = { x: 0, y: 1 };
		else if (k === 'ArrowLeft' || k === 'a') nd = { x: -1, y: 0 };
		else if (k === 'ArrowRight' || k === 'd') nd = { x: 1, y: 0 };
		if (nd) { e.preventDefault(); if (nd.x !== -dir.x || nd.y !== -dir.y) nextDir = nd; }
	}
	let tsx = 0, tsy = 0, touching = false;
	function setDir(nd) { if (!dead && (nd.x !== -dir.x || nd.y !== -dir.y)) nextDir = nd; }
	function onTouchStart(e) { const t = e.touches[0]; tsx = t.clientX; tsy = t.clientY; touching = true; e.preventDefault(); }
	function onTouchMove(e) {
		if (!touching) return; e.preventDefault();
		const t = e.touches[0], dx = t.clientX - tsx, dy = t.clientY - tsy, TH = 22;
		if (Math.abs(dx) < TH && Math.abs(dy) < TH) return;
		if (Math.abs(dx) > Math.abs(dy)) setDir({ x: dx > 0 ? 1 : -1, y: 0 });
		else setDir({ x: 0, y: dy > 0 ? 1 : -1 });
		tsx = t.clientX; tsy = t.clientY;
	}
	function onTouchEnd() { touching = false; }
	function start() {
		if (running) return;
		running = true; wrap.classList.add('show'); wrap.setAttribute('aria-hidden', 'false');
		reset();
		document.addEventListener('keydown', onKey, true);
		canvas.addEventListener('touchstart', onTouchStart, { passive: false });
		canvas.addEventListener('touchmove', onTouchMove, { passive: false });
		canvas.addEventListener('touchend', onTouchEnd);
		addEventListener('resize', fit);
		lastT = 0; raf = requestAnimationFrame(loop);
		toast(matchMedia('(pointer: coarse)').matches ? 'snake — swipe to steer' : 'snake — esc to quit', 1700);
	}
	function stop() {
		running = false; wrap.classList.remove('show'); wrap.setAttribute('aria-hidden', 'true');
		cancelAnimationFrame(raf);
		document.removeEventListener('keydown', onKey, true);
		canvas.removeEventListener('touchstart', onTouchStart);
		canvas.removeEventListener('touchmove', onTouchMove);
		canvas.removeEventListener('touchend', onTouchEnd);
		removeEventListener('resize', fit);
	}
	document.getElementById('snakeRetry').addEventListener('click', reset);
	document.getElementById('snakeClose').addEventListener('click', stop);
	return { start, stop, isRunning() { return running; } };
})();
document.getElementById('playLink').addEventListener('click', () => Snake.start());

/* ── keyboard: d = day/night · type "snake" ────────────────────────── */
(function () {
	let buf = '';
	addEventListener('keydown', e => {
		const t = e.target;
		if (t && (t.tagName === 'INPUT' || t.tagName === 'TEXTAREA' || t.isContentEditable)) return;
		if (Snake.isRunning()) return;
		if (e.metaKey || e.ctrlKey || e.altKey) return;
		const kl = e.key.toLowerCase();
		if (kl === 'd') { document.getElementById('modeToggle').click(); }
		if (/^[a-z]$/.test(kl)) {
			buf = (buf + kl).slice(-6);
			if (buf.endsWith('snake') || buf.endsWith('play')) { Snake.start(); buf = ''; }
		}
	});
})();
</script>

</body>
</html>
